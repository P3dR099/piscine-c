#include <stdio.h>

/*void functEj(int a, int b, int *div, int *mod)
{
	*div = (a / b);
	*mod = (a % b);
	printf("Division: %i", div);
	printf("Resto: %i", mod);
}
*/
int main()
{
	int a;
	int b;
	int *div;
	int *mod;
	div = (a / b);
    mod = (a % b);
    printf("Division: %i", *div);
    printf("Resto: %i", *mod);

}
